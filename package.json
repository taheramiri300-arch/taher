{
  "name": "qa-automation",
  "version": "1.0.0",
  "description": "QA Automation with Playwright + Newman + n8n",
  "scripts": {
    "test:playwright": "playwright test --reporter=json,html",
    "test:api": "newman run tests/api-collection.json --reporters json --reporter-json-export results/api-results.json",
    "test:all": "npm run test:playwright && npm run test:api"
  },
  "devDependencies": {
    "@playwright/test": "^1.43.0",
    "newman": "^6.1.2"
  }
}
